run-name: Test, build, and deploy the PR to main created by @${{ github.actor }} to staging environment

on:
  pull_request:
    types:
      - opened
      - synchronize
    branches:
      - main

jobs:
  deploy_to_staging:
    name: Deploy build artifact to staging environment
    environment: staging_environment
    # permissions:
    #   contents: read
    #   pull-requests: write
    runs-on: ubuntu-22.04
    steps:
      - name: Post image ID to PR
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GHA_PAT }}
          script: |
            const imageId = '${{ vars.APP_ENV }}';
            const varName = context.ref.replaceAll('/', '_');
            console.log(`ref: ${varName}`);
            const repoVars = await github.rest.actions.listRepoVariables({
              owner: context.repo.owner,
              repo: context.repo.repo
            });
            console.log(`repoVars: ${repoVars}`);
            const commentId = repoVars.data.find(v => v.name === varName)?.value;
            console.log(`commentId: ${commentId}`);
            if (commentId) {
              await github.rest.issues.updateComment({
                comment_id: Number(commentId),
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: `The Docker image ID for this build is: ${imageId}`
              });
            } else {
              const { data } = await github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: `The Docker image ID for this build is: ${imageId}`
              });
              console.log(`commentId2: ${data.id}`);
              await github.rest.actions.createRepoVariable({
                owner: context.repo.owner,
                repo: context.repo.repo,
                name: varName,
                value	: data.id.toString()
              });
            }
