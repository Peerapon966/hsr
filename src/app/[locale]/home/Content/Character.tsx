export type CharacterProps = {
  name: string;
  imagePath: string;
  worldIndex: number;
  charIndex: number;
};

export function Character({
  name,
  imagePath,
  worldIndex,
  charIndex,
}: CharacterProps) {
  return <div></div>;
}
